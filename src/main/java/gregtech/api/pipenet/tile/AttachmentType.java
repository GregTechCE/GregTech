package gregtech.api.pipenet.tile;

public enum AttachmentType {

    /**
     * Currently only used for rendering
     */
    COVER,

    /**
     * Used if the connection is actually there
     */
    PIPE
}
